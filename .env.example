# PostHog build-time configuration.
# These are read by the VersionGeneratorPlugin in composeApp/build.gradle.kts at build
# time via System.getenv(). If unset, the defaults embedded in the plugin are used
# (which point PostHog at the production project). Export these in your shell to
# override the defaults, e.g. to send events to a different PostHog project/host.
POSTHOG_API_KEY=phc_qs5bWGSxQhH5CpEcUEBJfbcUDKuLnzjLfP6MQZnEXSRB
POSTHOG_HOST=https://us.i.posthog.com
